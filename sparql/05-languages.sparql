SELECT DISTINCT ?item ?itemLabel
WHERE
{
  ?item wdt:P31/wdt:P279* wd:Q34770 .
  ?item wdt:P1098 ?speakers filter(?speakers > 100000000) .
  SERVICE wikibase:label { bd:serviceParam wikibase:language "en". }
}
GROUP BY ?item ?itemLabel
ORDER BY ?itemLabel